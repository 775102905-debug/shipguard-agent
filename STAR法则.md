# AI Delivery Inspector 项目 STAR 法则复盘

## S：Situation（背景）

近年来，以 Cursor、Trae 为代表的 AI 编程工具和 Vibe Coding 范式快速普及，开发者可以用自然语言在数小时内生成一个"看起来能跑"的项目原型。然而，这种高效率也带来了新的问题：

- 项目目录结构混乱，缺少 README、`.env.example`、`.gitignore` 等基础交付物；
- 代码中硬编码 API Key、Token、本地路径等安全隐患随处可见；
- 依赖配置不完整，版本未锁定，他人无法复现运行环境；
- Docker 部署材料缺失，项目难以移植和上线；
- 面试/作业/交付场景下，缺乏客观、可复现的质量评估手段。

行业急需一个**轻量、自动化的项目交付审查工具**，能够在不上传代码仓库、不运行代码的前提下，快速评估一个 AI 项目的交付质量。

## T：Task（任务）

设计并实现一个 **AI 项目交付审查官（AI Delivery Inspector）**，核心目标：

1. 用户上传一个 AI 项目源码 ZIP 包，系统自动解析项目结构；
2. 从交付完整性、安全风险、依赖配置、README 文档、Docker 部署、可维护性六个维度进行审查；
3. 输出结构化的 Markdown 审查报告，包含评分、风险等级、修复建议；
4. 支持四种审查模式：学生作业、GitHub 展示、面试项目、商业交付；
5. 保持 MVP 轻量：不引入数据库、不要求登录、不真实运行用户代码、不自动修复。

## A：Action（行动）

### 需求收敛与 MVP 边界

- 明确 V1 不做：数据库、登录、OAuth、沙箱运行、自动 PR、复杂 AST 分析；
- 技术选型：Python FastAPI + LangGraph 后端，React Vite + TypeScript 前端；
- 所有路径处理使用 `pathlib`，杜绝硬编码绝对路径。

### 后端实现（FastAPI + LangGraph）

- 7 个独立服务模块：zip 安全解压、项目解析、结构扫描、安全扫描、依赖审查、README 审查、报告生成；
- LangGraph 工作流编排，各节点可独立替换，预留 LLM 节点接口；
- 12 种安全扫描模式：sk- API Key、ghp_ Token、AKIA AWS Key、SECRET、PASSWORD、Bearer、Authorization、C:\Users\、/Users/、debug=True、DEBUG=True、CORS \*；
- ZIP 解压安全防护：校验 zip slip 路径穿越攻击；
- 评分体系：六维度 100 分制，自动判定 PASS / CONDITIONAL_PASS / REJECT。

### 前端实现（React + Vite + TypeScript）

- 简洁深色主题 UI，支持拖拽上传 ZIP 文件；
- 四种审查模式选择；
- 审查结果实时展示：总分、结论、问题统计、完整 Markdown 报告；
- 加载状态和错误提示。

### 测试与验证

- 构造 `good_project.zip`（完整交付物）和 `bad_project.zip`（含多种风险）测试用例；
- 编写 `scripts/smoke_test.py`，覆盖后端启动、health、good/bad zip 上传、报告生成和风险检出等关键链路；
- 经过三轮红律审查 PASS，确保代码质量和安全合规。

## R：Result（结果）

- **MVP v0.1 已完成**，前后端均可本地启动运行；
- **GitHub 已推送**，项目代码托管在远端仓库；
- **三轮红律审查全部 PASS**：
  - 第一轮：代码结构审查 PASS；
  - 第二轮：运行时联调验证 PASS；
  - 第三轮：前端联调与 E2E 验证 PASS；
- **good_project.zip 审查结果**：总分 85+，结论 PASS；
- **bad_project.zip 审查结果**：总分 27，结论 REJECT，检出 3 个高危、9 个中危、1 个低危问题；
- 扫描正确跳过 `node_modules`、`.git`、`__pycache__`、`dist` 等忽略目录；
- 发现 `.env` 文件仅报告风险，不读取和展示敏感内容。

---

## 后续可更新方向

- **白昼/黑夜主题切换**：当前仅深色主题，后续可支持用户切换；
- **Markdown 报告安全渲染**：Markdown 报告渲染安全性后续可引入更成熟的安全 Markdown 渲染库，避免潜在 XSS 风险；
- **50MB 上传后端强校验**：当前已有大小校验，可进一步细化错误提示和进度反馈；
- **异常响应/网络中断提示增强**：前端超时重试、断网检测、更友好的错误页面；
- **README 增加截图和演示说明**：补充功能截图、演示 GIF，提升项目可读性；
- **GitHub Actions / CI 审查工具**：将审查能力封装为 GitHub Action，支持 PR 自动审查。

---

## 面试口语版

### 30 秒版

> 我做了一个 AI 项目交付审查工具。用户上传项目 ZIP，系统自动检查缺少哪些文件、有没有硬编码密钥、README 是否完整，然后生成一份带评分的 Markdown 报告。后端用 FastAPI + LangGraph，前端用 React + Vite，完成了一个可运行 MVP，并通过三轮审查验证主链路。

### 2 分钟专业版

> 这个项目的出发点是：AI 编程工具普及后，很多人快速生成项目但交付质量参差不齐。我设计了一个自动审查系统，用户上传 ZIP 包，系统从六个维度打分：交付完整性、安全风险、依赖配置、README 质量、Docker 部署、可维护性。
>
> 技术实现上，后端用 Python FastAPI 提供 API，LangGraph 编排审查工作流，每个审查步骤是独立节点，方便后续扩展 LLM 增强分析。前端用 React + Vite + TypeScript，支持拖拽上传和四种审查模式。
>
> 安全和工程方面：解压做了 zip slip 防护，扫描跳过 node_modules 等目录，检测 12 种安全模式，.env 只报风险不读内容。我用 good/bad 两组测试项目做了端到端验证，覆盖后端启动、health、good/bad zip 上传、报告生成和风险检出等关键链路。
>
> 项目不依赖数据库、不需要登录，本地一条命令就能启动，很适合作为轻量审查工具或 CI 管道的前置检查。

---

## 答辩版总结

**各位老师好，我介绍的项目是 AI Delivery Inspector（AI 项目交付审查官）。**

项目的核心问题是：在 AI 辅助编程日益普及的今天，如何快速、客观地评估一个 AI 项目的交付质量？

我设计了一套基于规则的多维度审查系统。用户只需上传项目 ZIP，系统会自动完成以下工作：

1. 安全解压并定位项目根目录；
2. 生成项目画像，识别技术栈和框架；
3. 从结构完整性、安全风险、依赖配置、README 文档、Docker 部署、可维护性六个维度评分；
4. 最终输出一份可读的 Markdown 报告，包含评分、风险等级和修复建议。

技术选型上，我选择了 FastAPI + LangGraph 作为后端，React + Vite 作为前端，保持无数据库、无登录的轻量架构。整个审查流程通过 LangGraph 工作流编排，每个节点独立解耦，后续升级 LLM 增强节点只需替换对应模块，不需要重构整体架构。

在工程实践方面，我特别注重了三点：

- **安全**：zip slip 防护、.env 只报风险不读内容、12 类安全模式静态扫描；
- **可验证**：构造了 good/bad 两组测试项目，覆盖后端启动、health、good/bad zip 上传、报告生成和风险检出等关键链路；
- **边界清晰**：MVP 只做静态规则审查，不运行代码、不引入数据库、不自动修复，确保项目可快速交付。

项目前后端均已可本地运行，三轮红律审查全部 PASS。后续计划支持白昼/黑夜主题切换、增强异常处理、封装为 GitHub Action 用于 CI 自动审查。

以上是我的项目介绍，谢谢大家。
